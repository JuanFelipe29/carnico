<%= form_with(model: elaborated_product_name) do |form| %>
  <% if elaborated_product_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(elaborated_product_name.errors.count, "error") %> prohibited this elaborated_product_name from being saved:</h2>

      <ul>
        <% elaborated_product_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

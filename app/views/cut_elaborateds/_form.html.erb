<%= form_with(model: cut_elaborated) do |form| %>
  <% if cut_elaborated.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cut_elaborated.errors.count, "error") %> prohibited this cut_elaborated from being saved:</h2>

      <ul>
        <% cut_elaborated.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cut_id, style: "display: block" %>
    <%= form.text_field :cut_id %>
  </div>

  <div>
    <%= form.label :elaborated_product_name_id, style: "display: block" %>
    <%= form.text_field :elaborated_product_name_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

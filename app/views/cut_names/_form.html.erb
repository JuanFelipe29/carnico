<%= form_with(model: cut_name) do |form| %>
  <% if cut_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cut_name.errors.count, "error") %> prohibited this cut_name from being saved:</h2>

      <ul>
        <% cut_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :piece_id, style: "display: block" %>
    <%= form.collection_select :piece_id, Piece.includes(:piece_name).all, :id, :name, { prompt: 'Seleccione la pieza'} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

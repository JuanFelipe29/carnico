<p style="color: green"><%= notice %></p>

<h1>Cut names</h1>

<div id="cut_names">
  <% @cut_names.each do |cut_name| %>
    <%= render cut_name %>
    <p>
      <%= link_to "Show this cut name", cut_name %>
    </p>
  <% end %>
</div>

<%= link_to "New cut name", new_cut_name_path %>
